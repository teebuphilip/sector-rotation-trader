# Blocked Queue

This directory stores JSONL entries for algos blocked by missing API keys.

File:
- `algos.jsonl`

Each line is a JSON object with:
- `timestamp`
- `algo_id`
- `name`
- `keys`
- `reason`
